machine:
  node:
    version: 6.1.0
  services:
    - docker

deployment:
  pre:
    - docker build --rm=false -t absor/lovebot .
  hub:
    branch: master
    commands:
      - docker login -u $DOCKER_USER -p $DOCKER_PASS
      - docker push absor/lovebot
